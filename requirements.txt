runpod>=1.7.0
requests>=2.28.0
flask>=2.3.0
Pillow>=10.0.0
numpy>=1.24.0
rembg>=2.0.50
onnxruntime-gpu>=1.19.0
spandrel>=0.1.0
opencv-python-headless>=4.8.0

# Pin torch to match the RunPod PyTorch template (2.4.1+cu124).
# Without this, spandrel pulls latest torch (~2.10) which:
# 1. Downloads ~5GB extra (new torch + new CUDA libs)
# 2. Fills the 30GB pod disk to 104%
# 3. Ollama can't start (no disk space for temp files) → boot loop
torch==2.4.1
torchvision==0.19.1
